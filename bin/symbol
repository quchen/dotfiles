#!/usr/bin/env bash

set -euo pipefail

QUERY="$*"
case "$QUERY" in
    tm | trademark)
        echo "™"
        ;;
    "->")
        echo "→"
        ;;
    *)
        echo "Unrecognized symbol: $QUERY"
esac
