#!/usr/bin/env bash

set -euo pipefail

QUERY="$*"
case "$QUERY" in
    tm | trademark )
        echo "™"
        ;;
    "->" )
        echo "→"
        ;;
    check | checkmark )
        echo "✔"
        ;;
    *)
        echo "Unrecognized symbol: $QUERY"
esac
