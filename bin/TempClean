#!/usr/bin/env bash

# Removes files from ~/temp/ if they haven't been modified for X days.
# (gvfs-trash is Thunar's trash can.)
find $HOME/temp/* -maxdepth 0 -mtime +10 -print0 | xargs -t -0 trash

